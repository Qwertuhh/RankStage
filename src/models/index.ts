import { User } from "@/models/user.model";
import { Submission } from "@/models/submission.model";
import { Domain } from "@/models/domain.model";
import { Quiz } from "@/models/quiz.model";

export { User, Submission, Domain, Quiz };
